<% if user_signed_in? %>
  Logged in as <strong><%= current_user.email %></strong>.
  <br>
  <%= button_to('Logout', destroy_user_session_path, :method => :delete, :class => 'well')%>
<% end %>
<%= react_component("App") %>
